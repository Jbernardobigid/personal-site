temporary screenshots/
node_modules/
.env
carousels/
carousel-meta.json
carousel-usage.json
carousel-prepared.json
videos/
video-prepared.json
audio-work/
signals/
*.mp4
brand_assets/
mohamedshehata.net_.png
precificacao.md
.claude/
post-meta.json
post-caption.txt
newsletter-queue-state.json
ig-queue-state.json
metrics-history.json
ig-audit-report.json
ig-audit-report.md

# LinkedIn newsletter paste-staging (regenerable from blog posts) + launch copy
linkedin-newsletter/

# Harness / tooling noise
.agents/
skills-lock.json
assets/music/
assets/b-roll/

# Personal brand brain + strategy captures — repo is PUBLIC, keep these local
jorge_ai_reels_clone_studio/
brainstorms/
infra/

# Phase 4 cycling Reels — per-run concept + usage ledger (VPS-local state, survives git reset)
cycling-topic.json
cycling-topics-used.json
ig-performance.json

# Photo-day (single-image, non-Reel) daily cadence — per-run pick, same ledger convention
photo-topic.json

# Audience simulation — sim/personas.json, sim/content-variants.json, and the
# .mjs tooling are committed (methodology), but round data and the generated
# strategy outputs contain brand-partnership targeting analysis — repo is
# PUBLIC, keep these local, same tier as brainstorms/.
sim/sim-state.json
sim-predictions.json
docs/audience-simulation-report.md
temp/
projects/*/out/
